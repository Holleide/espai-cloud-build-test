main/
├── CMakeLists.txt          # SRCS: main.c + codetest/codetest.c
├── main.c                  # app_main → codetest_init → while(1) codetest_loop
└── codetest/
    ├── codetest.h          # 接口与宏（GPIO2 / 500ms）
    └── codetest.c          # GPIO 驱动实现